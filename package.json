{
  "name": "ethereumjs-abi",
  "version": "0.5.0",
  "description": "Decoder and encoder for the Ethereum ABI",
  "main": "index.js",
  "dependencies": {
    "bn.js": "^4.10.0",
    "ethereumjs-util": "^2.3.1",
    "utf8": "2.1.1"
  },
  "devDependencies": {
    "browserify": "12.0.1",
    "coveralls": "^2.11.4",
    "istanbul": "^0.4.1",
    "mocha": "^2.1.0",
    "standard": "^5.4.1"
  },
  "scripts": {
    "coverage": "istanbul cover _mocha",
    "coveralls": "npm run coverage && coveralls <coverage/lcov.info",
    "lint": "standard",
    "prepublish": "npm run lint && npm run test",
    "build": "browserify index.js -s EthAbi -o ethereumjs-abi.js",
    "test": "istanbul test node_modules/mocha/bin/_mocha -- --reporter spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axic/ethereumjs-abi.git"
  },
  "keywords": [
    "ethereum",
    "ABI"
  ],
  "author": "Alex Beregszaszi <alex@rtfs.hu>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/axic/ethereumjs-abi/issues"
  },
  "homepage": "https://github.com/axic/ethereumjs-abi",
  "standard": {
    "globals": [
      "describe",
      "it"
    ]
  }
}
